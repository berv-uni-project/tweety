matrix:
  include:
    - language: csharp
      mono: none
      dotnet: 2.1
      script:
        - cd $TRAVIS_BUILD_DIR/src/TweetyMVC
        - dotnet restore
        - dotnet build
        - dotnet publish
    - language: node_js
      node_js:
        - "10"
      before_script:
        - cd $TRAVIS_BUILD_DIR/src/tweety-ui
        - npm install
      script:
        - cd $TRAVIS_BUILD_DIR/src/tweety-ui
        - npm run lint
        - npm run build